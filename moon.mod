name = "brickfrog/moongate"

version = "0.1.0"

import {
  "moonbitlang/async@0.21.2",
  "moonbitlang/x@0.5.1",
}

description = "Semantic CI gatekeeper evaluating repository rules with TypeSafe Jev"

license = "Apache-2.0"

keywords = ["ci", "code-review", "github-action", "typesafe"]

source = "src"

preferred_target = "native"
